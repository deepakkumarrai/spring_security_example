<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
       xmlns:jpa="http://www.springframework.org/schema/data/jpa"
       xmlns:tx="http://www.springframework.org/schema/tx"
       xmlns="http://www.springframework.org/schema/beans"
       xsi:schemaLocation="http://www.springframework.org/schema/data/jpa
       http://www.springframework.org/schema/data/jpa/spring-jpa.xsd
       http://www.springframework.org/schema/tx
       http://www.springframework.org/schema/tx/spring-tx.xsd
       http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd">

    <!-- Define what is the source of data  -->
    <bean id="jpa.dataSource" class="org.springframework.jdbc.datasource.DriverManagerDataSource">
        <property name="username" value="${db.username:root}"/>
        <property name="password" value="${db.password:notasecret}"/>
        <property name="url" value="${db.url:jdbc:mysql://mysql-server/gsc7_test}"/>
        <property name="driverClassName" value="${db.driver:com.mysql.jdbc.Driver}"/>
    </bean>

    <!-- Map your data-source with Java Object (ORM Mapping) -->
    <bean id="jpa.factory" class="org.springframework.orm.jpa.LocalContainerEntityManagerFactoryBean">
        <property name="dataSource" ref="jpa.dataSource"/>
        <property name="packagesToScan" value="com.daffodilsw.core.data.jpa.model"/>
        <property name="persistenceUnitName" value="cms"/>
        <property name="jpaVendorAdapter">
            <bean class="org.springframework.orm.jpa.vendor.HibernateJpaVendorAdapter"/>
        </property>
        <property name="jpaProperties">
            <props>
                <prop key="hibernate.hbm2ddl.auto">${hibernate.hbm2ddl.auto:validate}</prop>
                <prop key="hibernate.dialect">${hibernate.dialect:org.hibernate.dialect.MySQL5InnoDBDialect}</prop>
                <prop key="hibernate.show_sql">${hibernate.show_sql:false}</prop>
                <prop key="hibernate.format_sql">${hibernate.format_sql:false}</prop>
                <prop key="hibernate.ejb.naming_strategy">fr.econsulting.gsc.core.data.jpa.utility.JpaNamingStrategy
                </prop>
                <prop key="hibernate.hbm2ddl.import_files">${hibernate.hbm2ddl.import_files:''}</prop>
                <prop key="hibernate.hbm2ddl.import_files_sql_extractor">
                    org.hibernate.tool.hbm2ddl.MultipleLinesSqlCommandExtractor
                </prop>
            </props>
        </property>
    </bean>

    <bean id="jpa.transaction" class="org.springframework.orm.jpa.JpaTransactionManager">
        <property name="entityManagerFactory" ref="jpa.factory"/>
    </bean>

    <jpa:repositories
            base-package="com.daffodilsw.core.data.jpa.repository"
            entity-manager-factory-ref="jpa.factory"
            transaction-manager-ref="jpa.transaction"
            />

    <jpa:auditing/>

    <tx:annotation-driven transaction-manager="jpa.transaction"/>

</beans>